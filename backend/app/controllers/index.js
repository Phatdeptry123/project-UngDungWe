const auth = require('./auth.controller')
const user = require('./users.controller')
const book = require('./book.controller')
const danhmuc = require('./danhmuc.controller')
module.exports = {
    auth,
    user,
    book,
    danhmuc,
}
